import type { SimulationContext } from '../context';
import type { PlayerInputs } from '../input';

export function abilitySystem(_ctx: SimulationContext, _inputs: PlayerInputs): void {
  // Rempli par la tâche 8: cast, cooldowns et activation des effets.
}
