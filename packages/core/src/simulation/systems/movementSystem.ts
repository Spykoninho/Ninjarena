import type { SimulationContext } from '../context';
import type { PlayerInputs } from '../input';

export function movementSystem(_ctx: SimulationContext, _inputs: PlayerInputs): void {
  // Rempli par la tâche 7: déplacement, terrain et collisions.
}
